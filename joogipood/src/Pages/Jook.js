import React from 'react'
import { useParams } from 'react-router-dom'

function Jook() {
    const { number } = useParams();

  return (
    <div>
        {leitud}
    </div>
  )
}

export default Jook